# 제출 안내 — AI·Data R&D Flow

CDX 사업담당자 개인 사업관리 워크스페이스에 **로컬 LLM 기반 보고 생성** 기능을 추가한 결과물입니다.

작성일: 2026-08-13

---

## 1. 주소

| 항목 | 주소 |
|---|---|
| **배포 사이트** | **https://rnd-flow.vercel.app** |
| GitHub 저장소 | https://github.com/eeejy/R-D_PM_tool |
| 작업 브랜치 | `feat/llm-daily-report` |
| Pull Request | https://github.com/eeejy/R-D_PM_tool/pull/1 |
| 로컬 프로젝트 경로 | `/Users/ej_air/R-D_PM_tool` |

배포 사이트는 설치 없이 바로 열립니다. **RFP 검색 · 연구개발 현황 · 업무트리 · 개요는 전부 동작합니다.**

> **LLM 기능만 배포 사이트에서 제한됩니다.** 브라우저가 공개 사이트에서 개인 컴퓨터 내부(`localhost`)로의
> 요청을 차단하기 때문입니다(Private Network Access). 보안상 의도된 제약이며 우회하지 않았습니다.
> 화면은 그대로 열리고, 모델 없이 **규칙으로 만든 문장**이 나옵니다.
> LLM까지 확인하시려면 아래 4번의 로컬 실행을 따라 주세요.

---

## 2. 추가한 기능

| 기능 | 하는 일 | 핵심 파일 |
|---|---|---|
| **일일 업무보고** | 등록된 업무 중 오늘 보고할 것을 골라 상부 구두보고용 / 연구책임자 전달용으로 나눠 문장화 | `lib/dailyReport.ts` |
| **이메일 초안** | 업무를 골라 발송용 협조요청 메일 초안 생성 | `lib/emailDraft.ts` |
| **회의록 분석** | 안건별 쟁점·결정·후속조치 추출 → 1페이지 보고서 → 업무로 등록 | `lib/minutes.ts` |
| **기관 마스터** | 9개 연구기관 + KIMST + 기타 = 11개 고정. 표기 변형을 하나로 모아 집계를 지킴 | `lib/org.ts` |
| (공통) | Ollama 호출 래퍼 · 응답 검증 | `lib/llm.ts` |

화면은 사이드바 **`보고 생성`** 메뉴에 있습니다.

### 설계 원칙 — 선별은 룰, 문장은 LLM

무엇을 보고할지 고르는 일은 기존 우선순위 점수(`lib/mytask.ts`)가 하고, LLM은 그것을 읽기 좋은
문장으로 바꾸는 역할만 합니다. 선별까지 모델에 맡기면 "왜 이게 보고사항인지"를 설명할 수 없게 됩니다.

- **로컬 전용** — 로컬 Ollama만 호출하고 클라우드 API는 쓰지 않습니다.
- **자동 호출 없음** — 버튼을 눌렀을 때만 모델이 돕니다.
- **근거 표시** — 생성된 문장 밑에 어느 업무·어느 안건에서 나왔는지가 붙고, `LLM`/`규칙` 배지로 출처를 구분합니다.
- **모델이 없어도 동작** — 연결이 없거나 응답이 깨져도 규칙 기반 결과로 떨어집니다.

### 환각을 막는 방식

코드가 통로 자체를 막았습니다.

- 일일 보고 — 입력 개수 = 출력 개수를 코드가 보장합니다. 모델이 없는 항목을 지어내면 버립니다.
- 이메일 — 기관명·날짜는 모델 출력이 아니라 원본 업무 데이터에서 주입합니다.
- 회의록 — 안건 번호(`sourceSegment`)는 모델이 아니라 호출한 쪽이 붙입니다. 모델이 돌려준 기한은
  **원문에 실제로 적혀 있을 때만** 받습니다.

---

## 3. 실제 검증

### 실제 사업공고 PDF (`AI기반 해양재난 대응체계 CDX 기술개발`)

RFP 검색 기능으로 확인했습니다. 32쪽 35,170자, 텍스트 추출 누락 없음.

| 검색어 | 결과 |
|---|---|
| `데이터` | 12쪽 / 110건 — 1위 p.27 (주요 연구개발내용) |
| `해양재난` | 12쪽 / 65건 — 1위 p.24 (주요 연구 범위) |
| `현장 실증` | 4쪽 / 12건 — 두 단어가 가까운 쪽이 상위 |
| `성과지표` | 3쪽 / 11건 |

### 실제 모델(qwen3:8b)로 확인하고 고친 것

- 회의록 머리말(회의명·일시·참석자)이 안건 1로 잡혀 안건 번호가 한 칸씩 밀림 → 머리말은 안건으로 세지 않도록 수정
- 모델이 "이달 말까지"를 `2026-08-31`로 바꿔 보냄 → 원문에 근거 없는 날짜를 버리는 검증 추가
- 공공문서의 `□`·`ㅇ` 글머리가 업무 제목에 딸려 와 분류가 밀림 → 글머리 제거

---

## 4. 로컬 실행 (LLM 포함 전체 기능)

```bash
# 1) 모델 준비 — 최초 1회
brew install ollama
ollama pull qwen3:8b

# 2) 모델 서버 실행 — 터미널 A (계속 켜 둠)
ollama serve

# 3) 앱 실행 — 터미널 B
cd /Users/ej_air/R-D_PM_tool
npm install
npm run dev
```

→ 브라우저에서 **http://localhost:3000** 접속 후 사이드바 `보고 생성`

Ollama 없이 실행해도 앱은 정상 동작합니다(보고 생성은 규칙 문장으로 출력).

### 테스트

```bash
npm test        # 287개 (기존 86개 + 신규 201개)
```

LLM이 생성한 **문장 자체는 테스트하지 않습니다.** 매번 달라지므로 대상이 될 수 없습니다.
대신 **스키마를 벗어난 응답이 왔을 때 앱이 죽지 않는지**를 테스트합니다 — 모델이 사과문을
돌려주든, 항목을 지어내든, 중간에 죽든 보고서는 나와야 합니다.

---

## 5. 파일 위치

```
/Users/ej_air/R-D_PM_tool
├── README.md              프로젝트 전체 설명 (설계 근거 포함)
├── SUBMISSION.md          이 문서
├── lib/                   도메인 로직 — React를 import하지 않음
│   ├── llm.ts               Ollama 호출 래퍼 · num_ctx/JSON 강제 · 응답 검증
│   ├── dailyReport.ts       보고 대상 선별 · 상부/연구책임자 분류 · 보고문 조립
│   ├── emailDraft.ts        메일 템플릿 · few-shot 프롬프트 · 기관·날짜 주입
│   ├── minutes.ts           안건 분할 · 안건별 추출 · 1페이지 보고서 · 업무 등록
│   ├── org.ts               기관 마스터 11개 · 별칭 매칭 · 회신 임계일
│   ├── history.ts           이벤트 로그 · 파생 지표 · 영업일 계산
│   ├── orgPage.ts           기관별 원페이지 집계 · 요약 문단
│   ├── reminder.ts          재촉 판정 · 단계별 문체 · 경위 주입
│   └── (기존) mytask · worktree · capture · rfp · wbs · nlp · store …
├── components/            화면
│   ├── AiView.tsx           보고 생성 셸 (연결 상태 · 설정 · 탭)
│   ├── AiDailyReport.tsx / AiEmail.tsx / AiMinutes.tsx
│   └── PromptPeek.tsx       모델에 들어가는 원문 미리보기
├── app/                   page.tsx (셸 + 상태) · globals.css (디자인 토큰)
└── tests/                 287개
```

상세한 설계 근거와 판단 기록은 **`README.md`** 에 있습니다.

---

## 6. 데이터 처리

모든 처리는 **브라우저 안에서만** 이뤄집니다. 업로드한 WBS·RFP·회의록은 서버로 전송되지 않고,
등록한 업무는 `localStorage`에만 저장되며 JSON으로 내보내고 되돌릴 수 있습니다.

LLM도 마찬가지입니다. 로컬 Ollama만 호출하고 클라우드 API는 붙이지 않았습니다. 무엇이 모델에
들어가는지 각 화면의 `모델에 보낼 원문 보기`로 미리 확인할 수 있습니다.

배포 사이트도 서버에 데이터를 보관하지 않습니다. 방문자 각자의 브라우저에만 데이터가 남습니다.

> **기관 도입 단계에서는 이것으로 부족합니다.** 회의록에는 기관 평가·지연 사유·발언자 실명 등
> 비공개 정보가 들어갑니다. 실제 투입 전에는 계정·MFA·역할 권한, 사업별 데이터 분리,
> 변경 이력과 감사 로그가 선택이 아니라 필수가 됩니다.

---

## 7. 알려진 한계

- **8B 모델로는 회의록 품질이 아쉽습니다.** 안건별 분할로 우회하고 있으며, 여유가 생기면
  `gpt-oss:20b`로 바꾸면 됩니다 — 모델명만 바꾸면 되므로 코드 변경이 없습니다.
- 팬 없는 노트북에서는 연속 호출 시 뒤로 갈수록 느려집니다.
- 메일 few-shot 예시가 아직 자리만 채운 기본값입니다. 실제로 보냈던 메일로 교체하면 공문체 품질이 오릅니다.
- 회의록에서 뽑은 후속조치는 담당·기한이 비어 있을 수 있습니다. 원문에 없으면 채우지 않기 때문이며,
  등록 후 사람이 채우는 것을 전제로 합니다.
